// okay, so we create a new ScrollGraph object.
// It has two SVGs sharing a common time axis.
/*

 Create a data object to hold the data. It knows its own start/end times, as well as its own min/max. It can be appended to,
 at which point, things like start/end and min/max are updated.


 */